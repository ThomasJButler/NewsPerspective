# IMPLEMENTATION_PLAN.md

## 1. Current status summary and code review
- Updated on 2026-03-12 after rechecking the remaining trusted-machine evidence flow from Codex and confirming the only unfinished work is still the real-key/manual-browser remainder.
- Accessible GitHub state in this environment:
  - `gh issue list -L 20` previously returned no open issues.
  - The open `V2.0` PR review remains the highest-signal external review artifact for this repo.
- Validation carried forward in this worktree:
  - Backend: `python3 -m unittest src.backend.tests.test_refresh_processing -v` passed.
  - Frontend: `cd src/frontend && npm run lint` passed.
  - Frontend: `cd src/frontend && npm run typecheck` passed.
  - Backend: `python3 -m unittest src.backend.tests.test_manual_integration_evidence -v` passed.
  - Backend: `python3 -m unittest src.backend.tests.test_config -v` passed.
  - Backend: `source src/backend/.venv/bin/activate && python -m src.backend.scripts.capture_manual_integration_evidence --output logs/phase3_manual_integration_report.md` passed.
  - Frontend: `cd src/frontend && npm run test:e2e:reuse -- tests/e2e/refresh-path.spec.ts` passed.
- Additional live-stack checks during this implementation pass:
  - Running `source .venv/bin/activate && uvicorn main:app --reload --port 8000` from `src/backend` failed with `ImportError: attempted relative import with no known parent package` because `main.py` was being imported as a top-level module instead of `src.backend.main`.
  - `printenv NEWS_API_KEY` still returned no value in this shell, so the helper still had no real key to use for the accepted-refresh/manual-browser scenarios.
  - `curl -I --max-time 5 http://localhost:3000` returned `HTTP/1.1 200 OK`, so the expected local frontend stack was reachable in this session.
  - `curl -sS --max-time 5 http://localhost:8000/api/articles` returned cached article data with `"total":774`, so the backend was already running and browseable without a key in this session.
  - `source src/backend/.venv/bin/activate && python -m src.backend.scripts.capture_manual_integration_evidence --output logs/phase3_manual_integration_report.md` wrote a fresh report that classified frontend reachability, cached browse without a key, and invalid-key handling as `code behavior`, while the real-key refresh start, duplicate-refresh-from-that-run, polling trail, and final completion state remained `still unproven` because no real key was supplied.
  - `cd src/frontend && npm run test:e2e:reuse -- tests/e2e/refresh-path.spec.ts` passed (`4 passed`), so the documented reuse-path browser command still works against the live local stack.
  - A direct Playwright browser check against `http://127.0.0.1:3000/` with empty `localStorage` confirmed the cached-browse UI contract: the feed rendered `774 articles processed · 243 headlines improved`, the inline `Fetch fresh headlines` onboarding card stayed visible, and pressing the refresh button without a saved key opened the settings dialog with the guidance text `No NewsAPI key is saved. Add one here or use the inline form before refreshing.` The screenshot for this check is `output/playwright/cached-browse-no-key-2026-03-12.png`.
  - `logs/phase3_manual_integration_report.md` now has the Codex-observable frontend follow-up rows filled in: the reuse-path Playwright row records `4 passed (8.6s)`, the cached-browse row records the exact stats and first visible headline/source, and the docs-drift row records `none`. Only the real-key refresh row remains intentionally unproven.
  - A 2026-03-12 runtime regression investigation found the backend running as `uvicorn main:app --reload --port 8000` from `src/backend`, which pointed the default cwd-relative SQLite URL at `src/backend/newsperspective.db` and made `/api/articles` appear empty (`total=0`) even though the populated cache remained in the repo-root database. Anchoring the default SQLite path in `src/backend/config.py` fixed the live app without changing explicit `DATABASE_URL` overrides.
  - The same startup pattern also hid the repo-root `.env`: because `SettingsConfigDict(env_file=".env")` was cwd-relative, launches from `src/backend` missed `/Users/tombutler/Repos/NewsPerspective/.env`, so `OPENAI_API_KEY` appeared unset and AI analysis fell back to neutral defaults. Anchoring the env-file path to the repo root fixed that path too.
  - A follow-up runtime check found the repo-root `.env` still contained `DATABASE_URL=sqlite:///./newsperspective.db`. Because that override is also cwd-relative, launches from `src/backend` still pointed at `src/backend/newsperspective.db` until `src/backend/config.py` started normalizing relative SQLite URLs to the repo root.
  - After the SQLite-path fix reloaded the backend, `curl http://localhost:8000/api/articles` returned cached processed articles again (`total=872` immediately after the fix, later `1019` after the helper refresh), `curl http://localhost:8000/api/stats` returned non-zero aggregates again, the helper report captured `code behavior` for accepted real-key refresh, duplicate short-circuit, polling, and terminal completion, and `~/.nvm/versions/node/v22.17.0/bin/npm run test:e2e:reuse -- tests/e2e/refresh-path.spec.ts` passed with `4 passed (10.9s)`.
- Verified implementation status in code:
  - `src/backend/main.py` now bootstraps the repo root onto `sys.path` when imported as plain `main`, so both documented package-style startup (`uvicorn src.backend.main:app ...`) and the common in-directory startup (`uvicorn main:app ...`) resolve the backend package correctly.
  - `src/backend/tests/test_api_smoke.py` now includes a regression check that loads `src/backend/main.py` as a top-level module and asserts the FastAPI app still initializes successfully.
  - `src/backend/config.py` now uses `SettingsConfigDict(..., extra="ignore")`, so unrelated env keys such as the trusted-machine `NEWS_API_KEY` no longer crash backend startup when Pydantic loads the local shell environment or `.env`.
  - `src/backend/tests/test_config.py` now proves `Settings()` tolerates `NEWS_API_KEY` in the environment while still honoring the documented `DATABASE_URL` override.
  - `src/backend/scripts/capture_manual_integration_evidence.py` now reads the real refresh key from the caller's `NEWS_API_KEY` environment variable when `--api-key` is omitted, and the generated report/checklist now tells the operator to export that variable before running the helper.
  - `src/backend/scripts/capture_manual_integration_evidence.py` now treats an invalid-key probe that hits `Refresh already in progress.` as `still unproven` rather than a documentation mismatch, because the backend never reached key validation in that case.
  - `README.md`, `AGENTS.md`, and `src/frontend/README.md` now describe one trusted-machine key-supply pattern: export `NEWS_API_KEY` in the local shell, let the helper read it from the environment, and use `curl --config - <<EOF` for direct refresh checks so the header value stays out of argv.
  - `src/backend/routers/articles.py` now orders `/api/articles` by `nulls_last(Article.published_at.desc())` and `Article.id.asc()`, and smoke coverage now proves stable pagination across tied and null `published_at` rows.
  - `src/backend/routers/sources.py` now redacts the user-supplied NewsAPI key from transport-level validation failures before returning the structured `502 upstream_transport_failure` response.
  - `src/backend/scripts/capture_manual_integration_evidence.py` now distinguishes an accepted refresh start from a duplicate `Refresh already in progress.` response, and it now treats HTTP `>=400` `/api/refresh/status` polls as backend-contract failures instead of collapsing them into "still unproven".
  - `src/frontend/tests/e2e/refresh-path.spec.ts` now mirrors backend `/api/sources` and `/api/stats` aggregation semantics for duplicate sources and `latest_fetch`, reuses a shared refresh-status payload builder, and the timeout path now asserts timeout behavior without coupling itself to a raw polling-cadence threshold.
  - `specs/BACKEND.md`, `src/frontend/README.md`, `AGENTS.md`, and `specs/OVERVIEW.md` now match the current codebase: backend retry/partial-failure coverage is described accurately, frontend Playwright docs mention both cached-browse and refresh-path flows, the AGENTS frontend validation block includes `npm run lint` plus `npm run typecheck`, and the overview architecture diagram is fenced as `text`.
  - `src/backend/tests/test_refresh_processing.py` now gives `_DummySession` explicit guard stubs for the session methods the processor path could reach, so unexpected test-double usage fails with a clear assertion instead of an `AttributeError`.
  - `src/frontend/app/article/[id]/page.tsx` no longer declares the redundant `articleImageLoader` or passes `loader={...}` to `Image` while also using `unoptimized`; the detail page still renders the optional image directly from `image_url`.
  - Processed-only visibility remains enforced across article list/detail, sources, and stats surfaces, with smoke coverage for pending/failed detail `404` behavior.
  - Structured refresh validation errors, duplicate-refresh short-circuiting at the API layer, tracker-state restoration after validation failure, and background-refresh regression coverage remain in place.
- Re-verified open implementation work that still remains:
  - [P2] The trusted-machine Phase 3 evidence pass still needs a real local `NEWS_API_KEY` plus the real-key browser follow-up row in `logs/phase3_manual_integration_report.md`; the Codex-observable no-key rows are now filled, but the credentialed/manual parts still cannot be completed inside Codex alone.

## 2. Active phase
Phase 3D is now effectively closed in the worktree: the trusted-machine evidence pass, reuse-path Playwright run, and manual real-key browser confirmation are all captured. The next loop should start Phase 4 with a plan-mode cleanup pass that removes stale Phase 3 blocker language from this file, promotes any missing product decisions into the plan/specs, and then hands off one concrete build slice. Keep the 2026-03-10 v2 boundary intact: if legacy v1 behavior needs reference, recover it from git history or `READMEOLD.md` instead of recreating root-level runtime files.

## 3. Ordered checklist with [ ] and [x]
- [x] [P1] Stabilize `/api/articles` pagination ordering in `src/backend/routers/articles.py`. Added the deterministic `Article.id.asc()` tiebreaker after `nulls_last(Article.published_at.desc())` and added backend regression coverage for tied and null `published_at` rows so offset pagination stays stable.
- [x] [P1] Fix `src/backend/scripts/capture_manual_integration_evidence.py:evaluate_refresh_start(...)` so duplicate `Refresh already in progress.` responses are classified as duplicate short-circuit behavior, not as successful new refresh starts. Added regression tests in `src/backend/tests/test_manual_integration_evidence.py` for both accepted-refresh and duplicate-refresh evaluation, and reused the same acceptance gate in `main()`.
- [x] [P1] Fix `src/backend/scripts/capture_manual_integration_evidence.py:evaluate_polling(...)` so HTTP `>=400` `/api/refresh/status` responses count as failures even when `_capture_response()` returned `ok=True`. Added regression tests proving those responses surface as backend-contract failures instead of "still unproven".
- [x] [P1] Redact the user-supplied NewsAPI key from refresh validation transport errors in `src/backend/routers/sources.py`. Added smoke coverage proving the structured `502` response no longer echoes the key.
- [x] [P1] Fix the refresh completion assertions in `src/frontend/tests/e2e/refresh-path.spec.ts` to use exact-match toast locators, then reran the targeted Playwright spec until the previously failing slice passed locally.
- [x] [P2] Bring `src/frontend/tests/e2e/refresh-path.spec.ts` fixtures in line with backend behavior. The mocked `/api/sources` and `/api/stats` builders now aggregate duplicate sources and compute `latest_fetch` the same way the backend does, refresh-status payload creation is centralized, and the timeout case now checks the user-visible timeout outcome plus polling shutdown instead of a raw cadence threshold.
- [x] [P2] Resolve the trusted-machine key-handling contract across docs and helper flow. `src/backend/scripts/capture_manual_integration_evidence.py` now reads `NEWS_API_KEY` from the caller environment when `--api-key` is omitted, the generated checklist tells operators to export that variable before running the helper, and `README.md`, `AGENTS.md`, and `src/frontend/README.md` now use the same trusted-machine pattern. Direct refresh examples now use `curl --config - <<EOF` so the header value does not appear in argv during local checks.
- [x] [P2] Align shipped docs/specs with current code: removed the stale backend coverage warning from `specs/BACKEND.md`; updated `src/frontend/README.md` to mention both cached-browse and refresh-path Playwright coverage; added `npm run lint` and `npm run typecheck` to the frontend validation guidance in `AGENTS.md`; labeled the ASCII diagram fence in `specs/OVERVIEW.md` as `text`.
- [x] [P2] Fix `src/backend/scripts/capture_manual_integration_evidence.py:evaluate_invalid_key(...)` so a duplicate `Refresh already in progress.` response is treated as `still unproven` instead of a documentation mismatch. Added a regression test in `src/backend/tests/test_manual_integration_evidence.py` and reran `python3 -m unittest src.backend.tests.test_manual_integration_evidence -v`.
- [x] [P1] Allow trusted-machine backend startup when `NEWS_API_KEY` is present in the local shell or `.env`. `src/backend/config.py` now ignores unrelated settings inputs via `extra="ignore"`, `src/backend/tests/test_config.py` covers `Settings()` with `NEWS_API_KEY` present, and `NEWS_API_KEY=dummy-key python -m unittest src.backend.tests.test_api_smoke -v` proved the app still boots and serves the backend smoke contract under that environment shape.
- [x] [P1] Anchor the default SQLite path to the repo root in `src/backend/config.py` so backend launches from `src/backend` and the repo root share the same cache. Added regression coverage in `src/backend/tests/test_config.py` to lock the default `DATABASE_URL` to `/Users/tombutler/Repos/NewsPerspective/newsperspective.db` unless explicitly overridden.
- [x] [P2] After the local fixes above land, finish the trusted-machine Phase 3 evidence pass on a real local machine: seed cached data first with `source src/backend/.venv/bin/activate && python -m src.backend.scripts.seed_manual_integration_data` if `/api/articles` is empty; run `python -m src.backend.scripts.capture_manual_integration_evidence` against the already running local stack using whatever key-supply pattern the previous checklist item standardizes; keep the same backend/frontend stack running and execute the exact reuse-path Playwright command printed in the generated report; open the frontend URL from the report, complete the frontend follow-up table with exact cached-browse and refresh outcomes, and paste the final observed results back into this file.
  - 2026-03-12 Codex live-stack recheck: the first helper run plus direct curl checks confirmed frontend reachability, cached browse without a key, and that the local stack can still surface duplicate-refresh collisions during evidence capture; `cd src/frontend && npm run test:e2e:reuse -- tests/e2e/refresh-path.spec.ts` passed. This item is still blocked on supplying a real `NEWS_API_KEY`, waiting for an idle/healthy backend window, and filling in the helper's manual browser follow-up table with exact observed UI outcomes.
  - 2026-03-12 Codex startup recheck: this loop found the frontend reachable but no backend listener on `localhost:8000`. Attempting to restart the backend exposed and then fixed a settings regression where a present `NEWS_API_KEY` prevented app startup; the remaining blocker is now back to the real-key/manual-browser evidence steps themselves.
  - 2026-03-12 Codex browser-and-helper recheck: this loop found both frontend and backend already running, and the regenerated helper report at `logs/phase3_manual_integration_report.md` now captures `code behavior` for frontend reachability, cached browse without a key, and invalid-key handling on the live stack. A direct Playwright browser pass with empty local storage confirmed that cached article cards still render alongside the inline `Fetch fresh headlines` onboarding card, and that clicking refresh without a saved key opens the settings dialog with the expected missing-key guidance. The remaining blocker is unchanged: no real `NEWS_API_KEY` was available in this Codex shell, so accepted real-key refresh, duplicate attach from that accepted run, refresh-status polling, final completion state, and real-key refresh UI outcomes are still unproven.
  - 2026-03-12 Codex report-fill recheck: this loop reran the helper and the documented reuse-path Playwright command, then replaced the report's generic frontend placeholders with exact observed no-key evidence. `logs/phase3_manual_integration_report.md` now records the `4 passed (8.6s)` reuse-path result, the cached-browse stats line `774 articles processed · 243 headlines improved`, the first visible cached headline/source (`Crowded field of candidates compete to replace MTG in Georgia special election - NPR` / `NPR`), the screenshot path `output/playwright/cached-browse-no-key-2026-03-12.png`, and `none` for docs drift. The only remaining TODO in that report is the real-key refresh UI row.
  - 2026-03-12 trusted-machine completion: after the SQLite/env-path fixes, the operator reran `python -m src.backend.scripts.capture_manual_integration_evidence --output logs/phase3_manual_integration_report.md`, reran `~/.nvm/versions/node/v22.17.0/bin/npm run test:e2e:reuse -- tests/e2e/refresh-path.spec.ts` (`4 passed (9.3s)`), and confirmed the browser success copy for the saved real key: `Refresh complete` and `No new articles were added this time.` The report now contains the completed frontend follow-up table and no remaining mismatch.
- [x] [P3] Apply the remaining low-risk cleanup after the higher-priority items above land: added defensive stub methods to `_DummySession` in `src/backend/tests/test_refresh_processing.py`, removed the redundant `articleImageLoader` / `loader={articleImageLoader}` pair from `src/frontend/app/article/[id]/page.tsx`, and reran the targeted backend/frontend proof set for those files.
- [x] [P3] For the documentation-only slice above, run the smallest proof set for the touched files: used targeted `rg` checks to confirm stale wording is gone; reran `python3 -m unittest src.backend.tests.test_refresh_processing -v` because `specs/BACKEND.md` changed; reran `cd src/frontend && npm run lint`, `cd src/frontend && npm run typecheck`, and `cd src/frontend && npm run test:e2e:reuse -- --list` because the frontend validation and Playwright guidance changed.
- [x] Backend smoke, manual-evidence, frontend typecheck, and the targeted refresh-path Playwright validations currently pass in this worktree.
- [x] Repo-managed and reuse-path Playwright entrypoints already exist, and the current suite still includes both cached-browse and refresh-path coverage.
- [x] The checked-out repo still reflects the v2 boundary: root-level v1 runtime files remain removed and legacy reference is preserved through history and `READMEOLD.md`.

## 4. Notes / discoveries that matter for the next loop
- The open PR review remains the highest-signal external review artifact, but the local P1 findings around pagination stability, refresh validation key redaction, manual-evidence classification, and refresh-path toast locators are now fixed in the worktree.
- The trusted-machine helper/docs contract now uses one supported local pattern: export `NEWS_API_KEY` in the caller shell, let `capture_manual_integration_evidence` read it from the environment, and use `curl --config - <<EOF` for direct refresh checks to keep the header value out of argv.
- `src/backend/config.py` now ignores unrelated env keys, which matters because the trusted-machine docs and helper intentionally rely on `NEWS_API_KEY` being present in the operator shell or local `.env`. Before this fix, that same variable could stop backend startup before any evidence capture began.
  - `src/backend/config.py` now also anchors the default SQLite URL to the repo root. This matters because launching `uvicorn main:app --reload --port 8000` from `src/backend` previously created and used `src/backend/newsperspective.db`, making the live app appear empty even though the populated cache lived at the repo root.
  - `src/backend/config.py` now also anchors the `.env` path to the repo root. This matters because launching from `src/backend` previously looked for `src/backend/.env`, which left `OPENAI_API_KEY` unset even when the real key existed in the repo-root `.env`.
  - `src/backend/config.py` now normalizes repo-local relative SQLite URLs such as `sqlite:///./newsperspective.db` to the repo root before the engine is created. This matters because the user's `.env` still overrides `DATABASE_URL`, and that override would otherwise keep sending launches from `src/backend` to the wrong database file.
- `refresh_start_was_accepted(...)` now gates both the manual-evidence scenario classification and whether duplicate/polling checks should run. If the first refresh call only hit an already-running refresh, the helper now correctly leaves the "successful refresh" scenario unproven instead of attributing somebody else's refresh to the current run.
- The invalid-key helper path has the same constraint: if `/api/refresh` is already busy and returns `Refresh already in progress.`, that response bypasses validation and should be treated as `still unproven`, not as docs drift. The helper and regression tests now encode that distinction.
- `HttpObservation.ok` still only means transport success. Any logic that needs to detect backend-contract failures must continue to inspect `status_code` explicitly; the helper now does this for refresh-status polling.
- `src/frontend/tests/e2e/refresh-path.spec.ts` now uses backend-aligned aggregation helpers for mocked `/api/sources` and `/api/stats`. `latest_fetch` still is not rendered in the current UI, so this alignment mainly protects future fixture accuracy rather than a current visible field.
- The shipped docs/specs now agree on two important operator details: the backend retry and partial-failure paths are covered in `src/backend/tests/test_refresh_processing.py`, and the frontend Playwright surface includes both cached-browse and refresh-path coverage.
- `AGENTS.md` now advertises the existing frontend `npm run lint` and `npm run typecheck` commands, and both commands currently exit cleanly in this worktree.
- `cd src/frontend && npm run test:e2e:reuse -- --list` currently enumerates 9 tests across `cached-browse.spec.ts` and `refresh-path.spec.ts`, which is enough to prove the documented reuse-path entrypoint still exposes both flows without requiring a live browser run for this doc slice.
- This Codex pass found the expected local backend/frontend stack already running. The first helper run plus direct curl checks captured partial live evidence for frontend reachability and cached browse without a key, and they also exposed the invalid-key duplicate-refresh classification gap that this slice fixed in the helper/tests.
- `cd src/frontend && npm run test:e2e:reuse -- tests/e2e/refresh-path.spec.ts` passed against that live stack (`4 passed`), so the documented reuse-path browser command is currently valid on this machine.
- Later follow-up helper/curl probes against the same backend timed out, so the trusted-machine evidence run should be captured on an idle local stack and pasted back into this file in one sitting instead of relying on partial follow-up probes.
- The latest helper rerun wrote `logs/phase3_manual_integration_report.md` with current live-stack evidence: frontend reachability, cached browse without a key, and invalid-key handling are now captured as observed `code behavior`; the real-key-only scenarios remain `still unproven` because this Codex shell still does not expose `NEWS_API_KEY`.
- A direct browser pass with empty local storage now verifies the visible cached-browse UX as well as the no-key refresh affordance: the page shows the inline `Fetch fresh headlines` card above cached article cards, and clicking the refresh button opens the settings dialog with `No NewsAPI key is saved. Add one here or use the inline form before refreshing.` The screenshot for that evidence is `output/playwright/cached-browse-no-key-2026-03-12.png`.
- The helper report now carries the exact Codex-observed frontend follow-up details for the no-key branch, including the reuse-path Playwright result (`4 passed (8.6s)`), the cached stats line `774 articles processed · 243 headlines improved`, the first visible cached headline/source, and `none` for docs drift. Only the real-key refresh UI outcome still needs a trusted-machine operator to fill it in.
- `_DummySession` in `src/backend/tests/test_refresh_processing.py` is now intentionally strict: `query`, `add`, `commit`, `refresh`, and `rollback` all raise an assertion if the supposedly unreachable DB path is touched in those tests.
- The article detail page still renders remote images with `unoptimized`, but it no longer carries a no-op loader wrapper around the same URL.
- A repo-wide search did not find the user's newer product rule about excluding `sports`, `entertainment`, and `politics` from the Good News experience in `IMPLEMENTATION_PLAN.md`, `specs/`, or the active frontend/backend source. That decision should be treated as missing source-of-truth and promoted into the next plan/spec update before implementation.
- The broader future roadmap is now captured in `specs/ROADMAP.md`, including country-aware reading, topic selection/hiding, accessibility, Fact Checker mode, feed-processing UX direction, the About modal, monetization constraints, and the Good News category exclusions. Keep those roadmap items visible, but do not let them displace the immediate next-loop sequence already listed in this plan.
- Trusted-machine evidence is still blocked inside Codex by the lack of a real `NEWS_API_KEY` and a real browser session. The mocked Playwright suite is useful contract coverage, but it does not replace the real local evidence pass.
- If the next trusted-machine pass starts from a shell or `.env` that includes `NEWS_API_KEY`, backend startup should now succeed instead of failing during settings parsing.
- This implementation pass did execute the helper against the running local stack, but without a real key it could not prove accepted refresh start, duplicate short-circuiting from that accepted run, refresh-status polling, terminal completion state, or the manual browser UI outcomes.
- Backend tests that set `DATABASE_URL` before importing app modules should continue to run in separate Python processes. Do not collapse `test_api_smoke` and `test_refresh_processing` into one interpreter invocation.
- Keep the legacy boundary intact. If upcoming work needs v1 behavior, recover it from git history or `READMEOLD.md`; do not recreate root-level runtime files as part of unrelated v2 cleanup.

## 5. Next recommended build slice
1. Plan-mode loop:
   Clean up `IMPLEMENTATION_PLAN.md` now that Phase 3 evidence is complete; remove stale blocker language, close Phase 3D explicitly, and add the missing product rule that `sports`, `entertainment`, and `politics` should not count toward the Good News experience unless a later spec explicitly reintroduces exceptions.
2. Build-mode loop:
   Add a visible refresh-status surface in the frontend that shows at least the latest successful refresh time and the latest refresh result summary (for example, completed with new article count, completed with no new articles, still running, or failed) using the existing backend stats/refresh-status data.
3. Follow-up build slice after the refresh-status surface:
   Implement the Good News category rule from the plan/spec cleanup by enforcing the excluded categories consistently in the backend classification/filtering path and adding regression coverage plus a small frontend proof that the Good News view no longer surfaces `sports`, `entertainment`, or `politics` stories.
